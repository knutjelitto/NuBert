using Pliant.Inputs;

namespace Pliant.Tokens
{
    public class Token : TokenBase
    {
        public Token(TokenClass tokenClass, Input start)
            : base(tokenClass, start)
        {
        }
    }
}
